global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'simulated-nodes'
    static_configs:
      - targets: ['node_exporter:9100']

  - job_name: 'sensor-exporter'
    static_configs:
      - targets: ['sensor_exporter:9200']
